# 贡献指南

## 开始之前

先阅读[产品需求](docs/PRODUCT_SPEC.md)、[系统架构](docs/ARCHITECTURE.md)和[技术栈](docs/TECH_STACK.md)。功能范围由产品需求决定，模块边界由架构文档决定；路线图不替代这两份规范。

应用和共享包的归属见[仓库结构](docs/WORKSPACE.md)。依赖在仓库根目录安装，安装、构建和测试命令以 `package.json#scripts` 为唯一事实来源。

## 变更要求

- 字幕处理逻辑保持为不依赖 DOM 和 Chrome API 的纯模块。
- 新增或修改字幕行为时，同时提交固定样本和自动化测试。
- Provider 测试使用本地模拟服务，不使用真实 API Key。
- 不在代码、日志、截图、fixture、Issue 或提交记录中放入密钥。
- 新增域名访问必须说明用途，并优先使用运行时 optional permission。
- 用户可见错误应说明发生了什么、发生位置和下一步操作。
- 修改模型输出结构时，同步更新 Schema、类型、校验器和故障测试。
- 修改提示词或断句语义时更新对应版本，使旧缓存可靠失效。

## 第三方代码

移植或改写第三方实现前，遵循[上游审计](docs/UPSTREAM_AUDIT.md)中的许可证执行清单。未经来源记录的复制不会被接受。

## 提交前检查

- 类型检查通过；
- 相关单元和集成测试通过；
- 扩展构建成功；
- 没有新增敏感信息或不必要权限；
- 行为、文档和界面文案保持一致。
